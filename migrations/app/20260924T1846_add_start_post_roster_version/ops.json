[
  {
    "id": "column.public.run_discord_post.lastStartRosterVersion",
    "label": "Add column \"lastStartRosterVersion\" to \"run_discord_post\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "lastStartRosterVersion",
        "table": "run_discord_post"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"lastStartRosterVersion\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "run_discord_post",
          "lastStartRosterVersion"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"lastStartRosterVersion\"",
        "sql": "ALTER TABLE \"public\".\"run_discord_post\" ADD COLUMN \"lastStartRosterVersion\" int4"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"lastStartRosterVersion\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "run_discord_post",
          "lastStartRosterVersion"
        ]
      }
    ]
  }
]