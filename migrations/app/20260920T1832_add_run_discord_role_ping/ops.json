[
  {
    "id": "column.public.run.discordRolePing",
    "label": "Add column \"discordRolePing\" to \"run\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "discordRolePing",
        "table": "run"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"discordRolePing\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "run",
          "discordRolePing"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"discordRolePing\"",
        "sql": "ALTER TABLE \"public\".\"run\" ADD COLUMN \"discordRolePing\" bool DEFAULT true NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"discordRolePing\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "run",
          "discordRolePing"
        ]
      }
    ]
  }
]