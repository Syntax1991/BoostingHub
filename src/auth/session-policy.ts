/** Better Auth 1.7.3 session.expiresIn — 30 days in seconds. */
export const SESSION_EXPIRES_IN_SECONDS = 60 * 60 * 24 * 30;

/** Better Auth 1.7.3 session.updateAge — refresh threshold while active (1 day). */
export const SESSION_UPDATE_AGE_SECONDS = 60 * 60 * 24;
